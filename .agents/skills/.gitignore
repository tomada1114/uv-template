# Generated Codex bridge symlinks (managed by bridge_symlink.sh).
# The project-local bridges are checked in so fresh clones discover them.
# Real Codex-only skills are NOT listed here and remain committable.
create-pr
merge-dependabot
release-workflow
smart-commit
